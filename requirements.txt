# Core training stack — versions aligned with pip deps in cl_env.yml.
# For a full reproducible env (CUDA, optional tools): conda env create -f cl_env.yml
accelerate==1.12.0
datasets==2.14.7
huggingface-hub==0.36.2
numpy==1.26.4
peft==0.18.1
safetensors==0.7.0
sentencepiece==0.2.1
tokenizers==0.22.2
torch==2.10.0
transformers==4.57.6
pytest>=8.0
# Optional: wandb (only if using --wandb-project)
# wandb==0.25.0
